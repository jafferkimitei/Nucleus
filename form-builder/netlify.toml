[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so client-side routing (added in a later phase) doesn't
# 404 on a hard refresh of a nested route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
