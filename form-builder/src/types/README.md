# types

Shared TypeScript types, chiefly the form/workflow schema contract
(`FormSchema`, `StepSchema`, `FieldSchema`, validation rule types) that every
other feature module renders from or edits. Lands in Phase 1.
