# features/builder

The drag-and-drop dashboard non-technical users build forms with:
`@hello-pangea/dnd` canvas, a field palette, and a property inspector that
edits the selected field's schema/validation. Produces the same
`FormSchema` the renderer consumes. Lands in Phase 4.
