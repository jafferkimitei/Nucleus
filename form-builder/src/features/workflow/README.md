# features/workflow

Multi-step navigation and the Zustand store backing it: current step,
per-field values, dirty/touched state, and conditional step branching
(skip/show a step based on prior answers). Lands in Phase 2.
