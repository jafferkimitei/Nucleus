# features/validation

Schema-driven validation: sync rules, cross-field rules, and async
conditional validation (e.g. Field B's visibility/validity depends on an API
check triggered by Field A), with debouncing and per-field error isolation.
Lands in Phase 3.
